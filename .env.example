# ---- Supabase (client-safe) ----
# Project Settings -> API -> Project URL
NEXT_PUBLIC_SUPABASE_URL=https://hegfrdgkhlxkkbhfayei.supabase.co
# Project Settings -> API -> Project API keys -> anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ---- Supabase (server-only, NEVER expose to client) ----
# Project Settings -> API -> Project API keys -> service_role
SUPABASE_SERVICE_ROLE_KEY=

# ---- Database (for running migrations only, server-only) ----
# Supabase dashboard -> Connect -> Session pooler (IPv4-friendly)
# postgresql://postgres.<ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres
DATABASE_URL=

# ---- App ----
# Public base URL used for OAuth redirects & invite links
NEXT_PUBLIC_SITE_URL=http://localhost:3000
